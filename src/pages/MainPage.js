import React from 'react';
import PropTypes from 'prop-types';
import AppFrame from '../components/AppFrame';

const MainPage = (props) => {
	return <AppFrame>Contenido</AppFrame>;
};

MainPage.propTypes = {};

export default MainPage;
